# Rough Idea

Design a TUI for the evidence collection agent.

Notes from the user:
- Streamlined PDD process — this doesn't have to be too comprehensive.
- Not the most important part of the project; doesn't have to be elaborate.

Context: the project currently has a minimal interactive REPL (`src/cli/repl.ts`) — type a task, watch it stream, get the run directory path. The agent drives a visible Chrome window and produces run directories with deliverables, manifest.json, transcript.jsonl, and metrics.json.
