andera-work-trial.md
.env
worklog.md
lab-notebook.md
docs/research/claude-code-harness.md
node_modules/
runs/
chrome-profile/
# Experiment records are the quantitative history — per-task accuracy and
# per-trial assertions/latency — and they are the only durable trace of a
# batch once its run dirs under runs/ are cleared. Small (~10KB each) and
# tracked deliberately.
